{
  "title": "TripAdvisor Scraper Input",
  "description": "Configuration for TripAdvisor listing scraping",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searches": {
      "title": "Search Terms",
      "type": "array",
      "description": "Search terms for TripAdvisor (e.g., ['hotels in Paris', 'restaurants in Tokyo'])",
      "items": { "type": "string" },
      "editor": "stringList"
    },
    "maxResults": {
      "title": "Maximum Results",
      "type": "integer",
      "description": "Maximum number of results to scrape",
      "default": 20,
      "minimum": 1,
      "maximum": 500
    }
  },
  "required": ["searches"]
}